name: Release Worker

on:
  workflow_call:

concurrency:
  group: release-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  work:
    runs-on: ubuntu-latest
    steps:
      - run: echo work
